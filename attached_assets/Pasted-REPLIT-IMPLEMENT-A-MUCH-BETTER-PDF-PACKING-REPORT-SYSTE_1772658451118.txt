REPLIT — IMPLEMENT A MUCH BETTER “PDF PACKING REPORT” SYSTEM (BRANDED + WITH 3D PLAN IMAGES)
PROJECT: AccessToNorth.com
FILE (MAIN): client/src/pages/tools/ContainerCalculator.tsx
GOAL: Replace the current “cheap/ugly” PDF with a professional, branded, compact report similar to Pier2Pier, but better.

REFERENCE (WHAT WE’RE TRYING TO BEAT):
- The competitor PDF has: header + logo, cargo table (L/W/H/weight/qty/color), a big container plan image with dimensions, and extra small plan views on page 2. :contentReference[oaicite:0]{index=0} :contentReference[oaicite:1]{index=1}

WHAT TO BUILD (NEW PDF REPORT V2):
A. PDF CONTENT (layout spec)
1) Header (top of every page)
   - Left: AccessToNorth.com logo (use existing site logo asset; if not available create a simple text logo fallback)
   - Center: “Container Packing Report”
   - Right: timestamp (local), reference / quote id (if user provided), and tool URL (optional)

2) Summary block (Page 1, under header)
   - Container type + internal dimensions + max payload
   - Total pieces, total weight, total volume (CBM + CFT), utilization % (weight + volume), number of containers used
   - Units shown according to user’s unit toggle (metric/imperial), but also show the other unit smaller in parentheses (optional)

3) Cargo table (compact, no giant spacing)
   Columns (minimum):
   - Item / Name
   - Qty
   - L / W / H
   - Weight per piece
   - Total weight
   - Stackable (Yes/No)
   - Rotation (e.g., “All”, “None”, or allowed orientations)
   - Color (small swatch)
   - Volume per piece + total volume (CBM/CFT)
   Add a totals row at bottom:
   - Totals: qty / weight / CBM / CFT

4) Load plan images (the “wow” part)
   - Page 1: one large “Isometric view” image of the packed container (like competitor)
   - Page 2: 2–4 smaller images:
     - Top view
     - Side view (right)
     - Side view (left) OR front/doors view
     - Optional: close-up view
   Each image should have a label and basic container dims text nearby (not huge).

5) Footer
   - Page X of Y
   - “Generated by AccessToNorth Container Loading Calculator”
   - Small disclaimer line: “Estimates only. Verify with carrier/warehouse.”

B. HOW TO GENERATE THE PDF (implementation approach)
Use @react-pdf/renderer for clean typography + consistent layout (preferred).
Alternative acceptable: jsPDF + html2canvas (but quality is usually worse).

Required steps:
1) Add dependency:
   - @react-pdf/renderer
   If already present, reuse.

2) Create a dedicated PDF component (new file preferred for cleanliness):
   - client/src/pages/tools/container-pdf/ContainerPackingReportPDF.tsx
   Export a function/component that takes:
   - containerSpec
   - cargoItems (with stackable/rotation/color)
   - placedBoxes (with positions/orientations)
   - summary metrics (computed)
   - images: { iso, top, sideA, sideB } as data URLs (PNG)

3) Capture images from the existing Three.js renderer
IMPORTANT: We already render the 3D view in ContainerViewer3D inside ContainerCalculator.tsx.
We need a reliable way to export snapshots.

Implement one of these options:
Option 1 (best): In ContainerViewer3D, store refs to:
   - renderer
   - scene
   - camera
Expose an “exportSnapshots()” callback to the parent via prop:
   - onReadyExport={(fn) => setExportFn(() => fn)}
The exportSnapshots() should:
   - Save current camera position/target
   - For each view (iso/top/side):
       * set camera position + lookAt target
       * renderer.render(scene, camera)
       * const dataUrl = renderer.domElement.toDataURL("image/png")
   - Restore camera
   - return { iso, top, sideA, sideB }

Camera presets (example):
   - iso: position (1.6, 1.0, 1.6) * containerScale
   - top: position (0, 2.5, 0) looking down
   - sideA: position (2.5, 0.7, 0)
   - sideB: position (-2.5, 0.7, 0)
Use container center as lookAt.

Ensure the canvas is NOT “tainted”:
   - Don’t use external textures without CORS.
   - Keep plain materials.

4) PDF download button behavior
Replace the current PDF generation button handler with:
   - Set “Generating PDF…” state
   - Call exportSnapshots() -> get images
   - Build a <PDFDownloadLink> or generate blob with pdf(<Component/>).toBlob()
   - Trigger download: “AccessToNorth_PackingReport_<date>.pdf”
   - Clear loading state

5) Make it fast and stable
   - Don’t re-render PDF on every keystroke.
   - Only generate on button click.
   - Cache snapshots if packing hasn’t changed.

C. VISUAL / DESIGN REQUIREMENTS (match site quality)
- Use the site font feel (clean, modern). In react-pdf you can use default fonts or register an included font if present.
- Keep margins consistent.
- Tables must be compact, readable, not huge.
- Add subtle section dividers.
- Colors: use AccessToNorth brand green for headings and separators (keep it subtle).

D. DATA REQUIREMENTS (what to include from our tool)
From current app state (ContainerCalculator.tsx), pass into PDF:
- Selected container type (IMPORTANT: use the user-selected container; do NOT “upgrade” container type automatically for reporting)
- Cargo list: name, dims, qty, weight, color, stackable, rotation mode
- Summary totals and utilization
- Number of containers used (if multi-container supported)

E. ACCEPTANCE CHECKLIST
- PDF includes AccessToNorth branding + looks “premium”
- Page 1 has summary + table + one big load plan image
- Page 2 has 2–4 smaller load plan images
- Totals row is correct (qty/weight/CBM/CFT)
- Works on desktop + mobile (download works)
- No laggy “selected item details” dependency (PDF generation must not rely on the laggy selection UI)

F. PLEASE IMPLEMENT NOW
1) Create the PDF component file
2) Add snapshot export from ContainerViewer3D (without breaking existing viewer)
3) Replace current PDF button handler with the new workflow
4) Test quickly with a sample packing (like 20’ DC and 40’ HC)

When done, tell me:
- What files you changed
- How to test (steps)
- If any limitation exists (e.g., snapshots quality / camera angles)